# 费曼学习平台 项目分析报告

> 基于《大前端与AI实战》课程 01 ~ 14

---

## 目录

1. 项目概览
2. 系统总体架构
3. 关键流程图
   - 登录鉴权
   - 知识点 CRUD
   - RAG 检索增强生成
   - 3D 可视化渲染
4. 数据模型（ER 图）
5. 亮点 & 待优化
6. 部署说明

---

## 1. 项目概览

| 维度 | 内容 |
|------|------|
| 项目名称 | **Feynman Learning Platform** |
| 技术栈 | Node.js · Express · MongoDB ・ React · Vite · Three.js · CesiumJS ・ DeepSeek / Qianfan Embedding |
| 核心功能 | 用户 & 认证 \| 知识点管理 \| 费曼记录 \| AI 润色/评测/出题 \| RAG 私有知识库 \| 知识图谱 & 3D 可视化 |

---

## 2. 系统总体架构

```mermaid
flowchart LR
  subgraph Browser[前端浏览器 (React)]
    A1[用户操作 UI]
    A2[Axios 调用 REST]
    A3[Three.js / CesiumJS 渲染]
  end

  subgraph API[后端 API (Express)]
    B1[/Auth 路由/]
    B2[/KnowledgePoint 路由/]
    B3[/FeynmanRecord 路由/]
    B4[/AI & RAG 路由/]
  end

  subgraph Services[业务服务层]
    C1[AuthService]
    C2[KnowledgeService]
    C3[AIService\n(DeepSeek)]
    C4[RAGService]
    C5[VectorStoreService]
  end

  subgraph Storage[持久化存储]
    D1[(MongoDB)]
    D2[(VectorStore JSON / Faiss)]
    D3[(Uploads S3/OSS)]
  end

  Browser -->|REST| API
  API --> Services
  Services -->|ORM| D1
  C5 --> D2
  A3 :::data -->|CDN| D3

  classDef data fill:#E8F6FF,stroke:#5694f0,stroke-width:1px;
```

---

## 3. 关键流程图

### 3.1 登录鉴权

```mermaid
sequenceDiagram
    participant U as 用户
    participant FE as 前端
    participant BE as 后端
    participant DB as MongoDB

    U->>FE: 输入用户名+密码
    FE->>BE: POST /api/users/login
    BE->>DB: 查询用户 & 验证密码
    DB-->>BE: 用户信息
    BE-->>FE: JWT Token
    FE-->>U: 设置 localStorage & 跳转 Dashboard
```

### 3.2 知识点 CRUD

```mermaid
flowchart TD
    C0[用户] -->|点击保存| C1[POST /knowledge-points]
    C1 --> C2[Auth Middleware]
    C2 --> C3[Controller]
    C3 --> C4[KnowledgeService]
    C4 --> C5[(MongoDB)]
    C5 -->|返回 _id| C3
    C3 -->|JSON| C0
```

### 3.3 RAG 检索增强生成

```mermaid
sequenceDiagram
    participant User
    participant FE
    participant RAG as RAG API
    participant VS as VectorStore
    participant LLM as LLM (DeepSeek/GPT)

    User->>FE: 提问 (query)
    FE->>RAG: POST /api/rag/ask
    RAG->>VS: similaritySearch(query)
    VS-->>RAG: top-k 文档 + metadata
    RAG->>LLM: prompt(query + context)
    LLM-->>RAG: answer
    RAG-->>FE: answer + sources
    FE-->>User: 渲染答案 & 高亮引用
```

### 3.4 3D 可视化渲染

```mermaid
flowchart LR
    P1[知识点数据] -->|REST| P2[React State]
    P2 --> P3[Three.js Scene]
    style P3 fill:#ffd;
    P3 -->|渲染| P4[WebGL]
    P2 --> P5[CesiumViewer]
```

---

## 4. 数据模型（ER 图）

```mermaid
erDiagram
    USERS ||--o{ FEYNMAN_RECORDS : "1:N"
    USERS ||--o{ KNOWLEDGE_POINTS : "1:N"
    KNOWLEDGE_POINTS ||--o{ FEYNMAN_RECORDS : "1:N"

    USERS {
      string _id PK
      string username
      string email
      string password
      date   createdAt
    }
    KNOWLEDGE_POINTS {
      string _id PK
      string title
      string contentMD
      string ownerId FK
      date   createdAt
    }
    FEYNMAN_RECORDS {
      string _id PK
      string explanationRaw
      string explanationPolish
      string audioUrl
      string knowledgePointId FK
      string ownerId FK
      date   createdAt
    }
```

---

## 5. 亮点 & 待优化

### 亮点
- 模块化 Controller / Service / Model —— 易扩展、可测试
- 支持 **RAG + 自定义向量库**，具备私有知识检索能力
- 3D / 地球 双引擎（Three.js & CesiumJS）可视化学习宇宙

### 待优化
1. 覆盖率 0 的自动化测试（Jest / Vitest）
2. 缺少集中式日志（可引入 pino + ElasticStack）
3. VectorStore 本地 JSON 不利于横向扩容 —— 推荐 Milvus / PGVector

---

## 6. 部署说明

```bash
# Backend
cd feynman-platform-backend
cp .env.example .env   # 填写 Mongo & Key
npm i && npm start

# Frontend
cd ../feynman-platform-frontend
npm i
npm run dev
```

亦可使用 `docker-compose.yml` 一键部署。

---

> © 2025 Feynman Learning Platform. All rights reserved.

